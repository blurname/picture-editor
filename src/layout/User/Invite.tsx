import React, {
  useState,
  useContext,
  useEffect,
  useCallback,
  useMemo,
} from 'react'
type inviteMsg={
	canvasId:number
	sure:boolean
}
export function Invite() {
const inviteList = useState([])
  return (
    <div>
	{j}
    </div>
  )
}
